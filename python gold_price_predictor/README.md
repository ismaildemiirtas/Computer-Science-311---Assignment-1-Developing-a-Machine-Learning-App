# Gold Price Prediction App

This project is a Python-based machine learning application that predicts the next-day closing price of gold using historical market data.

## How to Run
1. Install Python 3
2. Install dependencies:
   pip install -r requirements.txt
3. Run the application:
   python gold_price_predictor.py
